import java.util.Objects;public class Athlete implements Comparable<Athlete>{    private String raceNumber;    private String name;    private Time time;    public Athlete(String raceNumber, String name, Time time) {        this.raceNumber = raceNumber;        this.name = name;        this.time = time;    }    public String getRaceNumber() {        return raceNumber;    }    public String getName() {        return name;    }    public Time getTime() {        return time;    }    @Override    public boolean equals(Object o) {        if (this == o) return true;        if (o == null || getClass() != o.getClass()) return false;        Athlete athlete = (Athlete) o;        return Objects.equals(raceNumber, athlete.raceNumber);    }    @Override    public int hashCode() {        return raceNumber != null ? raceNumber.hashCode() : 0;    }    @Override    public int compareTo(Athlete o) {        if(o.equals(this)){            return 0;        }else if (o.time.getTotalSeconds() == this.time.getTotalSeconds()){            return o.raceNumber.compareTo(this.raceNumber);        }else{            return Integer.compare(o.time.getTotalSeconds(), this.time.getTotalSeconds());        }    }    // #659 00:17:52: Böhm Florian    @Override    public String toString() {        return String.format("#%s %s: %s",this.raceNumber, this.time.toString(), this.name);    }}